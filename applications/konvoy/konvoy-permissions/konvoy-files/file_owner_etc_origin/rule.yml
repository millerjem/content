documentation_complete: true

prodtype: konvoy5

title: 'Verify User Who Owns The Konvoy Configuration Directory'

description: '{{{ describe_file_owner(file="/etc/kubernetes/", owner="root") }}}'

rationale: 'If users can modify the Konvoy configurations, the Konvoy cluster can become inoperable or compromised'

severity: medium

ocil_clause: '{{{ ocil_clause_file_owner(file="/etc/kubernetes/", owner="root") }}}'

ocil: '{{{ ocil_file_owner(file="/etc/kubernetes/", owner="root") }}}'

template:
    name: file_owner
    vars:
        filepath: /etc/kubernetes/
        fileuid: '0'
