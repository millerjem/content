documentation_complete: true

prodtype: konvoy5

title: 'Verify Group Who Owns The Konvoy Configuration Directory'

description: '{{{ describe_file_group_owner(file="/etc/kubernetes/", group="root") }}}'

rationale: 'If users can modify the Konvoy configurations, the Konvoy cluster can become inoperable or compromised'

severity: medium

ocil_clause: '{{{ ocil_clause_file_group_owner(file="/etc/kubernetes/", group="root") }}}'

ocil: '{{{ ocil_file_group_owner(file="/etc/kubernetes/", group="root") }}}'

template:
    name: file_groupowner
    vars:
        filepath: /etc/kubernetes/
        filegid: '0'
