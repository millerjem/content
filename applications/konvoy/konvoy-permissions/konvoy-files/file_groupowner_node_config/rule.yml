documentation_complete: true

prodtype: konvoy5

title: 'Verify Group Who Owns The Konvoy Node config File'

description: '{{{ describe_file_group_owner(file="/etc/kubernetes/kubeadm-config.yaml", group="root") }}}'
rationale: |-
    The <tt>/etc/kubernetes/kubeadm-config.yaml</tt> file contains information about the configuration of the
    Konvoy node that is configured on the system. Protection of this file is
    critical for Konvoy security.

severity: medium

identifiers:
    cce@konvoy5: 80618-2

references:
    cis: 2.2.2

ocil_clause: '{{{ ocil_clause_file_group_owner(file="/etc/kubernetes/kubeadm-config.yaml", group="root") }}}'

ocil: '{{{ ocil_file_group_owner(file="/etc/kubernetes/kubeadm-config.yaml", group="root") }}}'

template:
    name: file_groupowner
    vars:
        filepath: /etc/kubernetes/kubeadm-config.yaml
        filegid: '0'
