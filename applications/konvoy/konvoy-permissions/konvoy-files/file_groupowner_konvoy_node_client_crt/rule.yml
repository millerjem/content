documentation_complete: true

prodtype: konvoy5

title: 'Verify Group Who Owns Konvoy Node Certificate File'

description: '{{{ describe_file_group_owner(file="/etc/kubernetes/pki/ca.crt", group="root") }}}'

rationale: |-
    The <tt>/etc/kubernetes/pki/ca.crt</tt> file contains the certificate authority
    certificate for an Konvoy node that is configured on the system. Protection of this file is
    critical for Konvoy security.

severity: medium

identifiers:
    cce@konvoy5: 80619-0

references:
    cis: 2.2.8

ocil_clause: '{{{ ocil_clause_file_group_owner(file="/etc/kubernetes/pki/ca.crt", group="root") }}}'

ocil: '{{{ ocil_file_group_owner(file="/etc/kubernetes/pki/ca.crt", group="root") }}}'

template:
    name: file_groupowner
    vars:
        filepath: /etc/kubernetes/pki/ca.crt
        filegid: '0'
