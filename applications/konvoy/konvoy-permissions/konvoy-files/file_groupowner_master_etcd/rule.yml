documentation_complete: true

prodtype: konvoy5

title: 'Verify Group Who Owns The Konvoy etcd Specification File'

description: '{{{ describe_file_group_owner(file="/etc/kubernetes/manifests/etcd.yaml", group="root") }}}'

rationale: |-
    The <tt>/etc/kubernetes/node/pods/apiserver.yaml</tt> file contains information about the configuration of the
    Konvoy etcd Server that is configured on the system. Protection of this file is
    critical for Konvoy security.

severity: medium

identifiers:
    cce@konvoy5: 80613-3

references:
    cis: 1.4.2

ocil_clause: '{{{ ocil_clause_file_group_owner(file="/etc/kubernetes/manifests/etcd.yaml", group="root") }}}'

ocil: '{{{ ocil_file_group_owner(file="/etc/kubernetes/manifests/etcd.yaml", group="root") }}}'

template:
    name: file_groupowner
    vars:
        filepath: /etc/kubernetes/manifests/etcd.yaml
        filegid: '0'
